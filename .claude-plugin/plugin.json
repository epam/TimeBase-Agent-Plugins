{
  "name": "timebase",
  "displayName": "TimeBase",
  "description": "Connect your AI agent to TimeBase, a high-performance database for financial market and other time-series data, via MCP tools and skills for QQL query and TimeBase client code generation.",
  "version": "0.1.4",
  "author": {
    "name": "EPAM Systems"
  },
  "homepage": "https://github.com/epam/TimeBase-Agent-Plugins",
  "repository": "https://github.com/epam/TimeBase-Agent-Plugins",
  "license": "Apache-2.0",
  "keywords": ["timebase", "time-series", "db", "qql", "financial-data"],
  "userConfig": {
    "timebase_url": {
      "type": "string",
      "title": "TimeBase URL",
      "description": "TimeBase connection URL",
      "required": true,
      "default": "dxtick://localhost:8011"
    },
    "timebase_username": {
      "type": "string",
      "title": "Username",
      "description": "Username for basic auth. Optional for OAuth2; when omitted, defaults to TIMEBASE_OAUTH2_CLIENT_ID"
    },
    "timebase_password": {
      "type": "string",
      "title": "Password",
      "description": "Password for basic auth. Mutually exclusive with OAuth2 settings",
      "sensitive": true
    },
    "timebase_read_only": {
      "type": "boolean",
      "title": "Read-only mode",
      "description": "Whether to open TimeBase connections in read-only mode"
    },
    "timebase_http_url": {
      "type": "string",
      "title": "TimeBase HTTP URL",
      "description": "Optional TimeBase HTTP base URL override"
    },
    "timebase_oauth2_token_url": {
      "type": "string",
      "title": "OAuth2 token URL",
      "description": "OAuth2 token endpoint for auth using the client credentials flow"
    },
    "timebase_oauth2_client_id": {
      "type": "string",
      "title": "OAuth2 client ID",
      "description": "OAuth2 client ID"
    },
    "timebase_oauth2_client_secret": {
      "type": "string",
      "title": "OAuth2 client secret",
      "description": "OAuth2 client secret",
      "sensitive": true
    },
    "timebase_oauth2_scope": {
      "type": "string",
      "title": "OAuth2 scope",
      "description": "Optional space-delimited OAuth2 scopes"
    },
    "timebase_oauth2_token_params": {
      "type": "string",
      "title": "OAuth2 token params",
      "description": "Optional JSON object of extra OAuth2 token form parameters"
    },
    "dxapi_ssl_termination": {
      "type": "boolean",
      "title": "SSL termination",
      "description": "Whether SSL termination is required when connecting to TimeBase"
    },
    "dxapi_ssl_cert_file": {
      "type": "file",
      "title": "SSL certificate file",
      "description": "Optional path to a DER certificate file"
    },
    "dxapi_ssl_trust_all": {
      "type": "boolean",
      "title": "Trust all SSL certificates",
      "description": "Whether SSL certificate verification should be disabled"
    },
    "mcp_host": {
      "type": "string",
      "title": "MCP host",
      "description": "Loopback host used for local interactive OAuth redirects",
      "default": "127.0.0.1"
    },
    "mcp_port": {
      "type": "string",
      "title": "MCP port",
      "description": "Loopback port used for local interactive OAuth redirects",
      "default": "8000"
    },
    "mcp_log_level": {
      "type": "string",
      "title": "Log level",
      "description": "Log verbosity: DEBUG, INFO, WARNING, ERROR, or CRITICAL",
      "default": "INFO"
    }
  },
  "mcpServers": {
    "timebase": {
      "command": "uvx",
      "args": [
        "--from",
        "timebase-mcp[all]==0.2.3",
        "timebase-mcp"
      ],
      "env": {
        "TIMEBASE_URL": "${user_config.timebase_url}",
        "TIMEBASE_USERNAME": "${user_config.timebase_username}",
        "TIMEBASE_PASSWORD": "${user_config.timebase_password}",
        "TIMEBASE_READ_ONLY": "${user_config.timebase_read_only}",
        "TIMEBASE_HTTP_URL": "${user_config.timebase_http_url}",
        "TIMEBASE_OAUTH2_TOKEN_URL": "${user_config.timebase_oauth2_token_url}",
        "TIMEBASE_OAUTH2_CLIENT_ID": "${user_config.timebase_oauth2_client_id}",
        "TIMEBASE_OAUTH2_CLIENT_SECRET": "${user_config.timebase_oauth2_client_secret}",
        "TIMEBASE_OAUTH2_SCOPE": "${user_config.timebase_oauth2_scope}",
        "TIMEBASE_OAUTH2_TOKEN_PARAMS": "${user_config.timebase_oauth2_token_params}",
        "DXAPI_SSL_TERMINATION": "${user_config.dxapi_ssl_termination}",
        "DXAPI_SSL_CERT_FILE": "${user_config.dxapi_ssl_cert_file}",
        "DXAPI_SSL_TRUST_ALL": "${user_config.dxapi_ssl_trust_all}",
        "MCP_HOST": "${user_config.mcp_host}",
        "MCP_PORT": "${user_config.mcp_port}",
        "MCP_LOG_LEVEL": "${user_config.mcp_log_level}"
      }
    }
  }
}
