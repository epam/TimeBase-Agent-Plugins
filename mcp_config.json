{
  "mcpServers": {
    "timebase": {
      "command": "uvx",
      "args": ["--from", "timebase-mcp[all]==0.2.2", "timebase-mcp"],
      "env": {
        "TIMEBASE_URL": "dxtick://localhost:8011"
      }
    }
  }
}
